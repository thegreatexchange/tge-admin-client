<nav class="navbar navbar-default navbar-fixed-top  {{if application.navbarHidden "hidden"}}">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="glyphicon glyphicon-menu-hamburger"></span>
      </button>
      {{#link-to "index" class="navbar-brand"}}<img alt="The Great Exchange" src="/admin/images/logo-sm.png" />{{/link-to}}
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      {{#if session.isAuthenticated}}
        <ul class="nav navbar-nav navbar-left">
          {{#if isAdminAppAuthorized}}<li><a href="/admin/">Admin</a></li>{{/if}}
          {{#if isEventAppAuthorized}}<li><a href="/event/">Event</a></li>{{/if}}
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li class="dropdown">
           <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user"></span> {{session.data.authenticated.email}}<span class="caret"></span></a>
           <ul class="dropdown-menu">
             <li> <a {{action 'invalidateSession'}}><span class="glyphicon glyphicon-log-out"></span> Sign out</a></li>
           </ul>
         </li>
        </ul>
      {{else}}
        <ul class="nav navbar-nav navbar-right">
          <li>{{#link-to "login"}}<span class="glyphicon glyphicon-log-in"></span> Sign in{{/link-to}}</li>
        </ul>
      {{/if}}
    </div>
  </div>
</nav>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12 main">
      {{#if session.isAuthenticated}}
        <div class="row">
          <div class="padding-vertical-34"></div>
          <div class="col-xs-12">
            <ul class="nav nav-tabs">
              {{#link-to "index" tagName="li"}}{{#link-to "index"}}<span class="glyphicon glyphicon-dashboard" aria-hidden="true"></span> Dashboard{{/link-to}}{{/link-to}}
              {{#link-to "events" tagName="li"}}{{#link-to "events"}}<span class="glyphicon glyphicon-calendar" aria-hidden="true"></span> Events{{/link-to}}{{/link-to}}
              {{#link-to "people" tagName="li"}}{{#link-to "people"}}<span class="glyphicon glyphicon-user" aria-hidden="true"></span> People{{/link-to}}{{/link-to}}
              {{#link-to "organizations" tagName="li"}}{{#link-to "organizations"}}<span class="glyphicon glyphicon-star" aria-hidden="true"></span> Organizations{{/link-to}}{{/link-to}}
            </ul>
            {{outlet}}
          </div>
        </div>
      {{else}}
        {{outlet}}
      {{/if}}
    </div>
  </div>
  <div class="row">
    {{flash-messages}}
  </div>
</div>

